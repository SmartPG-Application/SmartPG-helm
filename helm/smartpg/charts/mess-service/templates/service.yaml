apiVersion: v1
kind: Service
metadata:
  name: {{ include "mess-service.fullname" . }}
  labels:
    {{- include "mess-service.labels" . | nindent 4 }}
spec:
  type: ClusterIP
  selector:
    {{- include "mess-service.selectorLabels" . | nindent 4 }}
  ports:
    - protocol: TCP
      port: {{ .Values.service.port }}
      targetPort: {{ .Values.service.port }}
